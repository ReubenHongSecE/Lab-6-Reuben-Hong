//Task 3: Discovering Functions
//1. 
function addNumbers(num1, num2) { //declaring the function addNumbers and setting two variables as its paramters
    return num1 + num2; //2. returns the sum of both variables
}
//3.
console.log(addNumbers(5,6)); //calls the function, executes it and outputs based on the updated set of parameters

//4.
function multiplyNumbers(num1, num2) { //declaring the function multiplyNumbers and setting two variables as its paramters
    return num1 * num2; //returns the product of both variables
}

console.log(multiplyNumbers(3,4)); //calls the function, executes it and outputs based on the updated parameters
